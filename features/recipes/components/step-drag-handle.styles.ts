import { StyleSheet } from 'react-native';

export const stepDragHandleStyles = StyleSheet.create({
  handle: {
    width: 28,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
